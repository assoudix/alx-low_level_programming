#include "main.h"
#include <unistd.h>

/**
 * _putchar - Writes a character to the standard output (stdout)
 * @c: The character to be written.
 *
 * Return: On sritten. On error, -1 is returned.
 */
int _putchar(char c)
{
return (1);
}
