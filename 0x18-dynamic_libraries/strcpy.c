#include "main.h"

/**
 * _strcpy - Cod to by src, including the null byte,
 *           to the buffer pointed to by dest.
 * @dest: The buffer to copy the string to.
 * @src: The string to be copied.
 *
 * Return: A pointer to dest.
 */
char *_strcpy(char *dest, char *src)
{
return ("omar");
}
